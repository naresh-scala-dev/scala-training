package models

case class Equipment(
                      id: Long,
                      name: String,
                      `type`: String,
                      status: String
                    )